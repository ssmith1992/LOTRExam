@model List<Character>;

@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}


Thanks, for registering @ViewBag.Character.Name


<br /><br />

<h4>Here's some other Character suggestions with same 'Race' and ofc opposite 'Sex':</h4>

<div class="container">
    <div class="row">
    @foreach(var item in Model)
    {
      <div class="card col-12 col-sm-6 col-lg-3 col-xl-2">
        <img src="@Url.Content(item.ProfilePicture)" class="card-img-top" alt="...">
        <div class="card-body">
            <h4 class="card-title">@item.Name</h4>
            <h5 class="card-title">@item.races.Name</h5>
            <p class="card-text">@item.Description</p>
          </div>
        </div>
    }
    </div>
</div>